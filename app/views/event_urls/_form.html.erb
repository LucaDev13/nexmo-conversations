<%= form_for(event_url) do |f| %>
  <% if event_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_url.errors.count, "error") %> prohibited this event_url from being saved:</h2>

      <ul>
      <% event_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
