<%- model_class = NccoStreamAction -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:stream_url) %></th>
      <th><%= model_class.human_attribute_name(:loop) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @ncco_stream_actions.each do |ncco_stream_action| %>
      <tr>
        <td><%= link_to ncco_stream_action.id, ncco_stream_action_path(ncco_stream_action) %></td>
        <td><%= ncco_stream_action.stream_url %></td>
        <td><%= ncco_stream_action.loop %></td>
        <td><%=l ncco_stream_action.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ncco_stream_action_path(ncco_stream_action), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ncco_stream_action_path(ncco_stream_action),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_ncco_stream_action_path,
            :class => 'btn btn-primary' %>
