<%= form_for(ncco_stream_action) do |f| %>
  <% if ncco_stream_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ncco_stream_action.errors.count, "error") %> prohibited this ncco_stream_action from being saved:</h2>

      <ul>
      <% ncco_stream_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :stream_url %>
    <%= f.text_field :stream_url %>
  </div>

  <div class="field">
    <%= f.label :loop %>
    <%= f.text_field :loop %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
